# macOS noise
.DS_Store

# Local editor/session config
.claude/

# Local backups — never publish
*.bak

# Personal files not part of the public site (remove a line to publish it)
debt_tracker.html
SYNC_SETUP.md

# Bebot Brain lives in its own repo on a separate account — never publish here
bebot_brain/

# Bebot marketing site deploys to its own repo — never publish here
bebot_site/
